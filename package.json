{
  "name": "nova",
  "version": "0.1.0",
  "private": true,
  "description": "Shared infrastructure for the llamactl family — canonical AI-provider contracts and cross-cutting helpers for MCP servers. Monorepo housing @nova/contracts + @nova/mcp-shared.",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "typecheck": "bun run --cwd packages/contracts typecheck && bun run --cwd packages/mcp-shared typecheck",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "^1.3.12",
    "@types/node": "^25.6.0",
    "typescript": "^5.9.3"
  }
}
